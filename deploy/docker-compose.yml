services:
  laminara:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    image: laminara/laminara-server:latest
    restart: unless-stopped
    command: ["start", "--config", "/etc/laminara/config.json"]
    ports:
      - "127.0.0.1:8099:8099"
    volumes:
      - ./config.json:/etc/laminara/config.json:ro
      - laminara-data:/var/lib/laminara
    depends_on:
      redis:
        condition: service_started

  redis:
    image: redis:8-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

  # Включайте, только если адаптер аккаунтов читает базу, которую вы держите здесь.
  # postgres:
  #   image: postgres:18-alpine
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: laminara
  #     POSTGRES_PASSWORD: change-me
  #     POSTGRES_DB: laminara
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

  # Своё S3 — когда одного сервера мало. Установщик умеет прописать его сам;
  # варианты хранилища разобраны в документации, раздел «Хранилище».
  # garage:
  #   image: dxflrs/garage:v1.0.1
  #   restart: unless-stopped
  #   volumes:
  #     - ./garage.toml:/etc/garage.toml:ro
  #     - garage-meta:/var/lib/garage/meta
  #     - garage-data:/var/lib/garage/data

volumes:
  laminara-data:
  redis-data:
  # postgres-data:
  # garage-meta:
  # garage-data:
